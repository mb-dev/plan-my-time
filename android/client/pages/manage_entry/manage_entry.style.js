import React, {
  StyleSheet,
} from 'react-native';

const styles = StyleSheet.create({
  row: {
    paddingTop: 2,
    paddingBottom: 2,
  },
  toggleContainer: {
    flex: 1,
    flexDirection: 'row',
    marginBottom: 10,
    alignItems: 'center',
  },
  switchText: {
    color: 'black',
  },
});

export default styles;
