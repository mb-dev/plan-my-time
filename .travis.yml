language: python

python:
  - "3.5"

services:
  - mongodb

cache:
  directories:
    - node_modules
    - api/env

install:
  - "cd api; pip install -r requirements.txt"

before_script:
  - npm install --dev

script:
  - gulp test
